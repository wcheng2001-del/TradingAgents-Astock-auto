name: TradingAgents Astock Auto Analysis

on:
  workflow_dispatch:

concurrency:
  group: tradingagents-astock-auto
  cancel-in-progress: false

jobs:
  analyze:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Run analysis
        env:
          STOCK_LIST: ${{ vars.STOCK_LIST || '600519' }}
          TRADINGAGENTS_TRADE_DATE: ${{ vars.TRADINGAGENTS_TRADE_DATE }}
          TRADINGAGENTS_LLM_PROVIDER: ${{ vars.TRADINGAGENTS_LLM_PROVIDER || 'deepseek' }}
          TRADINGAGENTS_DEEP_THINK_LLM: ${{ vars.TRADINGAGENTS_DEEP_THINK_LLM || 'deepseek-v4-pro' }}
          TRADINGAGENTS_QUICK_THINK_LLM: ${{ vars.TRADINGAGENTS_QUICK_THINK_LLM || 'deepseek-v4-flash' }}
          TRADINGAGENTS_OUTPUT_LANGUAGE: ${{ vars.TRADINGAGENTS_OUTPUT_LANGUAGE || 'Chinese' }}
          TRADINGAGENTS_MARKET: ${{ vars.TRADINGAGENTS_MARKET || 'auto' }}
          TRADINGAGENTS_DATA_VENDOR: ${{ vars.TRADINGAGENTS_DATA_VENDOR || 'auto' }}
          TRADINGAGENTS_ANALYSTS: ${{ vars.TRADINGAGENTS_ANALYSTS }}
          TRADINGAGENTS_ASTOCK_ANALYSTS: ${{ vars.TRADINGAGENTS_ASTOCK_ANALYSTS }}
          TRADINGAGENTS_US_ANALYSTS: ${{ vars.TRADINGAGENTS_US_ANALYSTS }}
          TRADINGAGENTS_DEBUG: ${{ vars.TRADINGAGENTS_DEBUG || 'false' }}
          TRADINGAGENTS_RESULTS_DIR: reports/auto
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          DEEPSEEK_THINKING_ENABLED: ${{ vars.DEEPSEEK_THINKING_ENABLED || secrets.DEEPSEEK_THINKING_ENABLED }}
          DEEPSEEK_REASONING_EFFORT: ${{ vars.DEEPSEEK_REASONING_EFFORT || secrets.DEEPSEEK_REASONING_EFFORT }}
          DASHSCOPE_CN_API_KEY: ${{ secrets.DASHSCOPE_CN_API_KEY }}
          EMAIL_SENDER: ${{ vars.EMAIL_SENDER || secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ secrets.EMAIL_PASSWORD }}
          EMAIL_RECEIVERS: ${{ vars.EMAIL_RECEIVERS || secrets.EMAIL_RECEIVERS }}
          EMAIL_SENDER_NAME: ${{ vars.EMAIL_SENDER_NAME || secrets.EMAIL_SENDER_NAME || 'TradingAgents Astock Auto' }}
        run: python scripts/auto_batch.py

      - name: Upload reports
        uses: actions/upload-artifact@v6
        if: always()
        with:
          name: tradingagents-astock-reports-${{ github.run_number }}
          path: reports/
          retention-days: 30
